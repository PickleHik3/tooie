# Prefix
set -g prefix C-Space
set -g prefix2 C-b
bind C-Space send-prefix

# Reload config
bind q source-file ~/.tmux.conf
bind i run-shell '$HOME/.config/tmux/show-cheatsheet-popup'

# Vi mode for copy
setw -g mode-keys vi
bind -T copy-mode-vi v send -X begin-selection
bind -T copy-mode-vi y send -X copy-selection-and-cancel

# Pane Controls
bind h split-window -v -c "#{pane_current_path}"
bind v split-window -h -c "#{pane_current_path}"
bind x kill-pane

bind -n C-M-Left select-pane -L
bind -n C-M-Right select-pane -R
bind -n C-M-Up select-pane -U
bind -n C-M-Down select-pane -D

bind -n C-M-S-Left resize-pane -L 5
bind -n C-M-S-Down resize-pane -D 5
bind -n C-M-S-Up resize-pane -U 5
bind -n C-M-S-Right resize-pane -R 5

# Window navigation
bind r command-prompt -I "#W" "rename-window -- '%%'"
bind c new-window -c "#{pane_current_path}"
bind k kill-window
bind -n M-z last-window

bind -n M-1 select-window -t 1
bind -n M-2 select-window -t 2
bind -n M-3 select-window -t 3
bind -n M-4 select-window -t 4
bind -n M-5 select-window -t 5

bind -n M-Left select-window -t -1
bind -n M-Right select-window -t +1
bind -n M-S-Left swap-window -t -1 \; select-window -t -1
bind -n M-S-Right swap-window -t +1 \; select-window -t +1

# Session controls
bind R command-prompt -I "#S" "rename-session -- '%%'"
bind C new-session -c "#{pane_current_path}"
bind K kill-session
bind P switch-client -p
bind N switch-client -n

bind -n M-Up switch-client -p
bind -n M-Down switch-client -n
bind -n M-Enter run-shell '$HOME/.config/tmux/open-apps-popup'
bind -n MouseDown1StatusRight if-shell -F '#{==:#{mouse_status_range},launch}' "run-shell '$HOME/.config/tmux/open-apps-popup'" ''

# General
set -g default-terminal "tmux-256color"
set -g terminal-overrides ",xterm-256color:RGB,tmux-256color:RGB"
set -g mouse on
set -g base-index 1
setw -g pane-base-index 1
set -g renumber-windows on
set -g history-limit 50000
set -g escape-time 0
set -g focus-events on
set -g set-clipboard on
set -g allow-passthrough off
setw -g aggressive-resize on
set -g detach-on-destroy off

# Status bar
set -g status-position top
set -g status-interval 5
set -g status-left-length 30
set -g status-right-length 120
set -g window-status-separator ""

# Pane Borders single/double/heeavy/simple.
set -g pane-border-lines single

# >>> MATUGEN THEME START >>>
# Generated by /data/data/com.termux/files/home/.config/tooie/theme apply
set -g status-style "bg=default,fg=#e1e2e8"
set -g @status-left-style-session "#[fg=#000000,bg=#9999a5,bold]"
set -g @status-left-style-prefix "#[fg=#000000,bg=#96819a,bold]"
set -g @status-left-style-copy "#[fg=#000000,bg=#889dac,bold]"
set -g status-left "#{?client_prefix,#{@status-left-style-prefix} PRFX ,#{?pane_in_mode,#{@status-left-style-copy} COPY ,#{@status-left-style-session} #{session_name} }}#[fg=#e1e2e8,bg=default] "
set -g status-right "#(\$HOME/.config/tmux/run-system-widget all)#(\$HOME/.config/tmux/widget-weather)"
set -g window-status-separator ""
set -g window-status-format "#[fg=#ffffff,bg=#4f5b66] #I:#W "
set -g window-status-current-format "#[fg=#e8e8ed,bg=#99b9da,bold] #I:#W "
set -g pane-border-style "fg=#e1e2e8"
set -g pane-active-border-style "fg=#e1e2e8"
set -g message-style "bg=default,fg=#e1e2e8"
set -g message-command-style "bg=default,fg=#e1e2e8"
set -g mode-style "bg=default,fg=#000000"
setw -g clock-mode-colour "#a6c0d2"
set -g @status-tmux-palette "default"
set -g @status-tmux-widget-battery "on"
set -g @status-tmux-widget-cpu "on"
set -g @status-tmux-widget-ram "on"
set -g @status-tmux-widget-weather "on"
set -g @status-tmux-color-separator "#baadcf"
set -g @status-tmux-color-weather "#cfc7e1"
set -g @status-tmux-color-charging "#c6aaca"
set -g @status-tmux-color-battery-1 "#c3a8b0"
set -g @status-tmux-color-battery-2 "#b5b4c1"
set -g @status-tmux-color-battery-3 "#a6c0d2"
set -g @status-tmux-color-battery-4 "#a2bed5"
set -g @status-tmux-color-battery-5 "#9ebcd8"
set -g @status-tmux-color-battery-6 "#99b9da"
set -g @status-tmux-color-cpu-1 "#95b7dd"
set -g @status-tmux-color-cpu-2 "#a1b4d8"
set -g @status-tmux-color-cpu-3 "#aeb1d4"
set -g @status-tmux-color-cpu-4 "#baadcf"
set -g @status-tmux-color-cpu-5 "#c6aaca"
set -g @status-tmux-color-cpu-6 "#cab8d6"
set -g @status-tmux-color-ram-1 "#cfc7e1"
set -g @status-tmux-color-ram-2 "#d3d5ed"
set -g @status-tmux-color-ram-3 "#d7e3f8"
set -g @status-tmux-color-ram-4 "#d9cede"
set -g @status-tmux-color-ram-5 "#dcb9c3"
set -g @status-tmux-color-ram-6 "#dea4a9"
# <<< MATUGEN THEME END <<<
